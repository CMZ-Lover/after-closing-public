{
  "name": "after-closing",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "art:proof": "node --experimental-strip-types tools/art-proof.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^22.20.3",
    "typescript": "~5.7.3",
    "vite": "^6.3.1",
    "vitest": "^3.2.0"
  }
}
